# LPO reportGenerator

Python script to generate Word reports from GeoNature LPO databases.

## Usage

```bash
poetry install
poetry run reportgenerator --service <mon_service_pg> --output <mon_rapport.docx> --dataset <mon_id_dataset>
```